# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Vendored Tesseract runtime (worker, WASM core, ~18 MB of language models).
# Regenerated by `npm run vendor:tesseract`, which also runs on predev/prebuild,
# so a fresh clone and every production build get them automatically.
public/tesseract/

# Secrets. .env holds live PayPal credentials and must never be committed.
# .env.example carries the variable names only and is intentionally tracked.
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
